import {ColorTokens} from './TokensDocs'

# Colors Tokens
This React Library set color-tokens defined in two levels: global tokens and local tokens.
Global tokens are the most atomic tokens available, local tokens will aply some global tokens.
<ColorTokens />
